# NUnit Handson 3 - UtilLib (Custom attributes)

Source: `3__NUnit-Handson.docx`

## What this demonstrates
- `ParseHostName` has two execution paths (valid http/https vs. anything else), each with its own
  test method as required.
- `TestFixture`, `SetUp`, `TestCase`, `Assert.That()`.

## Run
```bash
dotnet test UtilLib.Tests
```
