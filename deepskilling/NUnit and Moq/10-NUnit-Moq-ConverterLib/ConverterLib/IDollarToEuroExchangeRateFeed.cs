namespace CurrencyConverterApp
{
    // Recreated for this repo: the original hand-on sources this interface from a separate
    // CurrencyConverterApp.zip download, which wasn't included in the uploaded project bundle.
    public interface IDollarToEuroExchangeRateFeed
    {
        double GetActualUSDollarValue();
    }
}
